box: python
build:
    steps: 
        - pip-install:
            requirements-file: ""
            packages_list: "swiftly"
        - script:
            name: echo python information
            code: |
                echo "python version $(python --version) running"
                echo "pip version $(pip --version) running"
                pip freeze